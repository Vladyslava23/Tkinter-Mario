#04.02.2025 Vladyslava Bilokha

import tkinter as tk
import ctypes

def main():

    aken = tk.Tk()
    aken.title("Vladyslava ülesanded")
    aken.geometry("300x300")
    aken.resizable(True, False)

    label = tk.Label(aken, text="Tere, maailm!").pack()
    button = tk.Button(aken, text="Sulge", command=aken.destroy).pack()
   
    aken.mainloop()

if __name__ == "__main__":
    main()
